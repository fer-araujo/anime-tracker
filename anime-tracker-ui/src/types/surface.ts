/**
 * The tone of a page's ambient backdrop.
 *
 * `primary` is the brand green, for everything that belongs to the user —
 * lists, collections, favourites, recommendations. The four seasons are for the
 * calendar. The hue says whose surface it is, not which page it is on.
 */
export type SurfaceHue = "primary" | "winter" | "spring" | "summer" | "fall";
